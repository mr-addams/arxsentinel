// ========================== Модуль state/tracker ========================================
//   In-memory состояние по IP: счётчики запросов, коды ответов, пути, timestamps.
//   Основа для всех детекторов — они читают состояние, а не сырые строки лога.
//
//   ЧТО ЗДЕСЬ:
//     - IPState — структура состояния одного IP
//     - Tracker — thread-safe хранилище состояний
//     - GC: очистка неактивных IP по таймеру, LRU eviction при max_tracked_ips
//
//   Реализуется в Task 2.1 и Task 2.2.

package state
