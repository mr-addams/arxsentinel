general:
  log_file: logs/haproxy-proxy/access.log
  pid_file: /tmp/arxsentinel-haproxy-proxy.pid
  tail_retry_interval: "2s"

parser:
  profile: haproxy-http

output:
  threat_log: logs/threats/haproxy-proxy.log
  operational_log: logs/threats/sentinel-haproxy-proxy.log

logging:
  console_color: false

blocklist:
  storage: ""
  lists:
    - name: badbot-ua
      refresh_interval: 24h
      sources:
        - url: "http://localhost:8090/badbot-ua.list"
          format: plain_text
    - name: badbot-ref
      refresh_interval: 24h
      sources:
        - url: "http://localhost:8090/badbot-ref.list"
          format: plain_text
