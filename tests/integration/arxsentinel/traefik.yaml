general:
  log_file: logs/traefik/access.log
  pid_file: /tmp/arxsentinel-traefik.pid
  tail_retry_interval: "2s"

parser:
  profile: traefik

output:
  threat_log: logs/threats/traefik.log
  operational_log: logs/threats/sentinel-traefik.log

logging:
  console_color: false
