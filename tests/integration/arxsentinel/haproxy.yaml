general:
  log_file: logs/haproxy/access.log
  pid_file: /tmp/arxsentinel-haproxy.pid
  tail_retry_interval: "2s"

parser:
  profile: haproxy-http

output:
  threat_log: logs/threats/haproxy.log
  operational_log: logs/threats/sentinel-haproxy.log

logging:
  console_color: false
