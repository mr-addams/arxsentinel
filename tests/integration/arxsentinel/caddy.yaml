general:
  log_file: logs/caddy/access.log
  pid_file: /tmp/arxsentinel-caddy.pid
  tail_retry_interval: "2s"

parser:
  profile: caddy

output:
  threat_log: logs/threats/caddy.log
  operational_log: logs/threats/sentinel-caddy.log

logging:
  console_color: false
