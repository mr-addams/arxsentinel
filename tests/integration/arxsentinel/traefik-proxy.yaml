general:
  log_file: logs/traefik-proxy/access-proxy.log
  pid_file: /tmp/arxsentinel-traefik-proxy.pid
  tail_retry_interval: "2s"

parser:
  profile: traefik

output:
  threat_log: logs/threats/traefik-proxy.log
  operational_log: logs/threats/sentinel-traefik-proxy.log

logging:
  console_color: false

blocklist:
  storage: ""
  lists:
    - name: badbot-ua
      refresh_interval: 24h
      sources:
        - url: "http://localhost:8090/badbot-ua.list"
          format: plain_text
    - name: badbot-ref
      refresh_interval: 24h
      sources:
        - url: "http://localhost:8090/badbot-ref.list"
          format: plain_text
