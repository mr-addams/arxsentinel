/var/log/arxsentinel/threats.log
/var/log/arxsentinel/sentinel.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    # copytruncate: MVP — does not require SIGHUP-reload of file descriptors.
    # Known limitation: race window ~1ms between copy and truncate (acceptable for a security log).
    # Proper postrotate: kill -HUP $(cat /var/run/arxsentinel.pid) — Task 7.1.
    copytruncate
}
