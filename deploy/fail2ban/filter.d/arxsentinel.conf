[INCLUDES]
before = common.conf

[Definition]
_daemon = nginx-sentinel

# Match lines of the form: THREAT 1.2.3.4 score=85 ...
# The scorer has already issued a verdict — every THREAT line is a confirmed threat.
failregex = ^%(__prefix_line)sTHREAT <HOST> score=\d+

ignoreregex =
