[INCLUDES]
before = common.conf

[Definition]
_daemon = nginx-sentinel

# Ловим строки вида: THREAT 1.2.3.4 score=85 ...
# Scorer уже вынес вердикт — каждая строка THREAT это подтверждённая угроза.
failregex = ^%(__prefix_line)sTHREAT <HOST> score=\d+

ignoreregex =
